/* p   Path to file or directory to start from
 * i   Comma-separated list of file or directory names to ignore, in string or regex format
 * e   Comma-separated list of file extensions to read from
 */

const config = {
  //p: "../path/to/project"
  //i: [".someDir", /some?regex?/, "someFile.js"]
  //e: ".js, .jsx, .ts, .tsx"
};

module.exports = config;
