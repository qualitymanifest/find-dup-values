/* p   Path to file or directory to start from
 * i   Array of file or directory names to ignore
 * I   Same as -i, but takes globs instead of strings
 * e   Array of file extensions to read from
 */

const config = {
  //p: "../path/to/project",
  //i: [".someDir", "someFile.js"],
  //I: ["*test*", "*spec.js"],
  //e: [".js", ".jsx", ".ts", ".tsx"]
};

module.exports = config;
